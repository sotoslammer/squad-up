import 'package:flutter/material.dart';

class CharacterSelectorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('Character Selector Screen')
    );
  }

}