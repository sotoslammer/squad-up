import 'package:flutter/cupertino.dart';

class Rosters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Center(
            child: Column(
      children: [Text('Empty Rosters')],
    )));
  }
}
